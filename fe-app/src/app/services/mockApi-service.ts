import { Injectable } from "@angular/core";
import { MockApi } from "@models/mockApi";

@Injectable()
export class MockApiService {
    GetAllMockApis() : []MockApi {
        const name = new type(arguments);
        return name
    }
}